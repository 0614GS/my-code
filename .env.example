# my-code 不会自动加载 .env；请由 shell、direnv 或进程管理器导出所需变量。
# Provider、模型、Base URL 和 API key 请通过首次启动向导或 /provider 配置，
# 不再支持 ANTHROPIC_*、OPENAI_*、MY_CODE_PROVIDER 和 MY_CODE_API_KEY。

# 可选：将用户设置、Provider 凭据、Session 和项目状态移出 ~/.my-code。
# MY_CODE_CONFIG_DIR=/path/to/private/my-code-state

# 可选：通过 OTLP/HTTP 导出 traces 和 metrics。未配置端点时不会发送遥测。
# 通用端点会由 OpenTelemetry exporter 补全 /v1/traces 和 /v1/metrics。
# OTEL_EXPORTER_OTLP_ENDPOINT=http://127.0.0.1:4318
# OTEL_SERVICE_NAME=my-code

# 可选：分别配置 signal 端点。启用时必须至少配置 traces 端点；若使用这一组，
# 建议同时配置 metrics，避免 exporter 回退到其默认地址。
# OTEL_EXPORTER_OTLP_TRACES_ENDPOINT=http://127.0.0.1:4318/v1/traces
# OTEL_EXPORTER_OTLP_METRICS_ENDPOINT=http://127.0.0.1:4318/v1/metrics

# 可选：Collector 认证与超时（秒）；由 OpenTelemetry OTLP/HTTP exporter 读取。
# OTEL_EXPORTER_OTLP_HEADERS=authorization=Bearer%20your-token
# OTEL_EXPORTER_OTLP_TIMEOUT=10

# 高敏感选项：导出提示词、模型输出和工具内容；默认关闭，生产环境慎用。
# MY_CODE_OTEL_CAPTURE_CONTENT=0
